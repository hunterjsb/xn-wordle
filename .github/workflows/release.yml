name: Build xn-wordle

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Build binaries
        run: |
          GOOS=linux GOARCH=arm64 go build -o xn-wordle-linux-arm64 .
          GOOS=linux GOARCH=amd64 go build -o xn-wordle-linux-amd64 .

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            xn-wordle-linux-arm64
            xn-wordle-linux-amd64
